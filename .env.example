# MongoDB credentials (required for docker-compose prod)
# Used to secure MongoDB - only needed when running with docker compose --profile prod
MONGO_USERNAME=admin
MONGO_PASSWORD=change_me_to_a_strong_password

# Database connection string
# Local dev (with `docker compose up mongo -d`): use 127.0.0.1, no auth
# Production (payload in Docker): docker-compose overrides with MONGO_USERNAME/MONGO_PASSWORD
DATABASE_URL=mongodb://127.0.0.1:27017/your-database-name

# Or use a PG connection string
#DATABASE_URL=postgresql://127.0.0.1:5432/your-database-name

# Used to encrypt JWT tokens
PAYLOAD_SECRET=YOUR_SECRET_HERE

# Used to configure CORS, format links and more. No trailing slash
NEXT_PUBLIC_SERVER_URL=http://localhost:3000

# Secret used to authenticate cron jobs
CRON_SECRET=YOUR_CRON_SECRET_HERE

# Used to validate preview requests
PREVIEW_SECRET=YOUR_SECRET_HERE

# Cloudflare R2 (S3-compatible) storage for media uploads
# When set, media uploads go to R2 instead of local storage
# R2_ENDPOINT format: https://<account_id>.r2.cloudflarestorage.com
# R2_REGION is typically 'auto' for R2
# R2_BUCKET=
# R2_ACCESS_KEY_ID=
# R2_SECRET_ACCESS_KEY=
# R2_ENDPOINT=
# R2_REGION=auto
